import React from 'react'; export interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> { error?: string; errorId?: string; } export const Input = React.forwardRef<HTMLInputElement, InputProps>(({ className = '', error, errorId, id, ...props }, ref) => { const derivedErrorId = error ? (errorId || (id ? id + '-error' : undefined)) : undefined; const baseBorder = error ? 'border-rose-500 focus:ring-rose-500' : 'border-slate-300'; const inputClass = 'w-full px-3 py-2 border rounded-md text-sm text-slate-900 bg-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-cyan-700 focus:border-transparent transition-colors disabled:bg-slate-50 disabled:text-slate-500 ' + baseBorder + ' ' + className; return <div className='w-full'><input ref={ref} id={id} aria-invalid={!!error} aria-describedby={derivedErrorId} className={inputClass} {...props}/>{error && <p id={derivedErrorId} className='mt-1 text-xs text-rose-600' role='alert'>{error}</p>}</div>; }); Input.displayName = 'Input';
